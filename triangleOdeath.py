def main():
    point = [0, 0]
    top = [0, 3]
    left = [-sqrt(3), [0, 0]]
    right = [sqrt(3), [0, 0]]
    for theta in range(60):
        slope = arctan(theta)
        y = [slope, point]

        for i in range(1001):
            if intersect(y, top) != None:


def intersect(y1, y2):
    if (intersect):
        return intersction
    else:
        return None


def reflection(y1, y2):
    line = [slope, point]
    return line
